@{
    // This partial view previously listed FluidBalanceAdministrationDto rows.
    // That table has been replaced by FluidBalanceChartEntry (the 24-hour table).
    // Kept as an empty stub so any lingering partial-view calls don't 404.
}